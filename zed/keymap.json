// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  // Some common shortcuts for a VsCode like experience
  {
    "context": "Editor && !menu",
    "bindings": {
      "ctrl-c": "editor::Copy", // vim default: return to normal mode
      "ctrl-x": "editor::Cut", // vim default: decrement
      "ctrl-v": "editor::Paste", // vim default: visual block mode
      "ctrl-y": "editor::Undo", // vim default: line up
      "ctrl-f": "buffer_search::Deploy", // vim default: page down
      "ctrl-o": "workspace::Open", // vim default: go back
      "ctrl-a": "editor::SelectAll", // vim default: increment
      "ctrl-w": "pane::CloseActiveItem" // vim default: close tab
    }
  },
  {
    "context": "VimControl && !menu",
    "bindings": {
      "space e": "file_finder::Toggle",
      "space o": "outline::Toggle",
      "space t": "workspace::ToggleLeftDock",
      "space w": "project_panel::ToggleFocus"
    }
  },
  {
    "context": "VimControl && Editor",
    "bindings": {
      "space yy": "vim::Yank",
      "space pp": "vim::Paste"
    }
  },
  {
    "context": "ProjectPanel",
    "bindings": {
      "space t": "workspace::ToggleLeftDock",
      "space w": "project_panel::ToggleFocus"
    }
  },
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-l": "terminal_panel::ToggleFocus"
    }
  },
  {
    "bindings": {
      "ctrl-`": "workspace::ToggleBottomDock",
      "ctrl-l": "terminal_panel::ToggleFocus"
    }
  }
]
